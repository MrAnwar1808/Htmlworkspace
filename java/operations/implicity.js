
let x = 4;
let y =2;

console.log( 7+'8' );              // 78 (if we give '+' it wil join the number instead of adding)
console.log( x + 'y' );

console.log('7' - 2);              // 5 (string converted to number and will do the '-' operation)
console.log( x - y );
console.log(true + 2);             // 3 (value of true is 1)
console.log(false + 2);            // 2 (value of false is 0)
